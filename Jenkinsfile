pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the app'
            }
        }
        stage('Unit tests') {
            steps {
                echo 'Testing the app'
            }
        }
        stage('Deploy Frontend') {
            steps {
                echo 'Deploying the app'
            }
        }
    }
}
